{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run WaddleTracker",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}"
      ],
      "outFiles": [
        "${workspaceFolder}/dist/**/*.js"
      ],
      "preLaunchTask": "WaddleTracker: Compile"
    }
  ]
}